/* const config = {
  plugins: [
    "@tailwindcss/postcss",
    "autoprefixer"
  ],
};

export default config;
 */

// postcss.config.mjs
const config = {
  plugins: {
    '@tailwindcss/postcss': {}, 
    'autoprefixer': {}, 
  },
};

export default config;